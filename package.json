{
  "type": "module",
  "version": "1.0.4",
  "scripts": {
    "build": "web-ext build",
    "lint": "eslint .",
    "sign": "web-ext sign",
    "start:firefox": "web-ext run --devtools",
    "version": "node version.js && git add . && npm run build"
  },
  "dependencies": {
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.2.2",
    "eslint": "^9.34.0",
    "eslint-plugin-format": "^1.0.1",
    "web-ext": "^8.9.0",
    "webextension-polyfill": "^0.12.0"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "eslint": "9"
      }
    }
  }
}
